import React from 'react';
import PageTemplate from 'components/base/PageTemplate';
import Header from 'components/base/Header';

const HotelsDiscovery = () => (
  <PageTemplate header={<Header />}>
    HotelsDiscovery
  </PageTemplate>
);

export default HotelsDiscovery;
